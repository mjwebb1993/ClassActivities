// select all unordered list (ul) elements
const ulList = document.querySelectorAll("ul");
console.log("Unorder List Elements", ulList);
// select all elements with the class "class-week"
const classWeekQuery = document.querySelectorAll(".class-week");
const classWeekGetClass = document.getElementsByClassName("class-week");

// select all elements with the class "nav-bar"
const navBar = document.querySelectorAll(".nav-bar");

// select the element with the id "dog-picture" and save it to a variable
const dogPicture = document.getElementById("dog-picture");
const dogPictureQuery = document.querySelector("#dog-picture");
